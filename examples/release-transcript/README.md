# Release Transcript Example

Build the offline launch transcript:

```bash
python tools/flowmemory_release_transcript.py --pretty
```

The transcript summarizes the local evidence stack, the pending public evidence
state, and the non-claims in one screenshot-ready output.
