# DischargeLine Example

Run:

```bash
python tools/dischargeline_harness.py demo --pretty
```

DischargeLine checks whether a receipt closes the right obligation. It keeps
payment settlement separate from obligation discharge.

It is not custody, escrow, wallet authorization, fund protection, work-quality
proof, semantic truth, model correctness, GPU acceleration, live mainnet
evidence, or production verifier infrastructure.
