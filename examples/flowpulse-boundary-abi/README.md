# FlowPulse Boundary ABI Example

Run the screenshot-ready ABI conformance gate:

```bash
python tools/flowpulse_boundary_abi.py check --pretty
```

Expected output is in [expected-output.txt](expected-output.txt).
