# SpendLine Example

Run:

```bash
python tools/spendline_harness.py demo --pretty
```

SpendLine checks whether autonomous agent spend claims can be accepted into a
receipt-bound memory history. It is not custody, wallet enforcement, or fund
protection.
