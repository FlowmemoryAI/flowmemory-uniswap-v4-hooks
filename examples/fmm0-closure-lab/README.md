# FMM-0 Closure Lab Example

Run the screenshot-ready closure demo:

```bash
python tools/fmm0_closure_lab.py demo --pretty
```

Expected output is in [expected-output.txt](expected-output.txt).

The demo checks that valid receipt-bound memory histories stay valid when
composed, while invalid composition is rejected.
