# FMM-0 Counterexample Forge Examples

This directory contains the expected output for the FMM-0 Counterexample Forge.

Run:

```bash
python tools/fmm0_counterexample_forge.py demo --pretty
```

The forge creates deterministic counterexamples from the phase-space fixtures
and checks that FMM-0 catches every one.
