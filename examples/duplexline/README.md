# DuplexLine Example

Run:

```bash
python tools/duplexline_harness.py demo --pretty
```

DuplexLine checks whether autonomous buyer/seller exchange claims can be
accepted into compatible receipt-bound memory histories. It is not custody,
escrow, wallet enforcement, fund protection, work-quality proof, or semantic
truth.
