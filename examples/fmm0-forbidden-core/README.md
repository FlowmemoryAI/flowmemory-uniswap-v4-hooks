# FMM-0 Forbidden Core Example

Run the screenshot-ready minimal-core demo:

```bash
python tools/fmm0_forbidden_core.py demo --pretty
```

Expected output is in [expected-output.txt](expected-output.txt).

The demo shrinks invalid FMM-0 histories into one-minimal forbidden cores.
