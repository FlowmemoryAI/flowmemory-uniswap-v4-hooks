# Public Canary Template

Use this when Base Sepolia deployment and reader evidence exist.

Do not publish this as a live claim until the blanks are filled with public evidence.

## Public Line

FlowMemory has a verified Base Sepolia hook deployment with observed FlowPulse logs and reader-derived receipt evidence.

## Short Public Post

FlowMemory introduces the first memory-native Uniswap v4 hook primitive: a verified on-chain emission boundary for FlowPulse memory signals.

Today we are publishing the Base Sepolia canary evidence for that primitive.

The swap transaction is not the memory.
The transaction is the proof envelope.
The FlowPulse is the memory artifact.

This hook does not custody funds, control swaps, route orders, alter fees, or change accounting. It emits a memory signal from the completed `afterSwap` boundary.

Evidence:

- Hook address:
- Source verification:
- Deployment transaction:
- Sample signal transaction:
- Sample `FlowPulse` log index:
- Reader evidence record:
- Release record:

Most hooks modify execution. FlowMemory emits memory.

## Evidence Table

| Field | Value |
| --- | --- |
| Network | Base Sepolia |
| Chain id | `84532` |
| Hook address | |
| PoolManager | |
| Source verification | |
| Deployment transaction | |
| Deployment block | |
| Reader from block | |
| Reader to block | |
| FlowPulse count | |
| AfterSwapObserved count | |
| Rejected log count | |
| Sample txHash | |
| Sample logIndex | |
| Finality status | |

## Boundary Statement

This is Base Sepolia evidence, not a Base mainnet production deployment claim.

Receipt metadata is reader-derived after transaction execution.

The hook emits the memory signal. The reader proves where it landed.

The transaction is the proof envelope. The FlowPulse is the memory artifact.
